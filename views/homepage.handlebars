{{#if posts.length}}
{{#each posts as |post|}}
<section class="post-card">
    {{> post-details post}}
    <a href="/post/{{id}}" class="post-link">Continue to post →</a>
</section>
{{/each}}
{{else}}
<section>
    <p>No posts have been made yet.</p>
</section>
{{/if}}